---
const { ariaLabel, href, className } = Astro.props;
---

<a
  class:list={["relative after:bottom-0 w-fit block after:block after:content-[''] after:absolute after:h-[.5px] after:bg-baseDarkGray after:w-full after:scale-x-0 after:hover:scale-x-100 after:transition after:duration-300 after:origin-left", className]}
  aria-label={ariaLabel}
  href={href}><slot /></a
>
